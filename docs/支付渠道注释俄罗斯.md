⏺ 现在应该可以了！我修改了前端的关键逻辑：

  1. getAvailablePaymentMethods() - 现在无论 isRussia 是什么，都只返回 Stripe 支付方式
  2. getRecommendedProvider() - 现在所有用户都推荐使用 Stripe

  这样，即使俄罗斯用户访问定价页面，也只会看到 Stripe 支付选项，不会再显示 SberPay、YooMoney 或 Mir Card 等俄罗斯支付方式。

  恢复方法：
  当俄罗斯支付开通后，只需要：
  1. 在 services/payment/PaymentProvider.ts 中恢复 shouldUsePayssion() 函数
  2. 在 lib/payment-methods.ts 中取消注释的两个函数